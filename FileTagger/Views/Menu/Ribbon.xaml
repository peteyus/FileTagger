<UserControl x:Class="FileTagger.Views.Menu.Ribbon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DataContext="RibbonMenuViewModel">
    <Ribbon x:Name="MainRibbon" Title="FileTagger">
        <Ribbon.ApplicationMenu>
            <!-- TODO PRJ: Icons -->
            <RibbonApplicationMenu x:Name="FileMenu">
                <RibbonApplicationMenuItem Header="Open" Command="{Binding OpenCommand}" />
                <RibbonApplicationMenuItem Header="Options" Command="{Binding OptionsCommand}" />
            </RibbonApplicationMenu>
        </Ribbon.ApplicationMenu>
    </Ribbon>
</UserControl>
